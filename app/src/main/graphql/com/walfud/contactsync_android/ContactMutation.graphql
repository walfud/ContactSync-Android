mutation SyncMutation($token: String!, $contacts: [ContactInputType]!) {
  sync(token: $token, contacts: $contacts) {
    contacts {
      id
      name
      phones
      modify_time
      is_deleted
    }
  }
}